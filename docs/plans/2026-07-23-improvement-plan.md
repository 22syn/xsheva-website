# Improvement plan — 2026-07-23

## Context
Static HTML/Vite/Tailwind marketing site on Firebase Hosting. Only stated want (recorded 2026-07-23, not yet built): content that reads as human-written, not AI-generated — same family as `Taste-Skill`/`stop-slop` from the repo shortlist. Note the repo already has a `graphify-out/` directory — Graphify (a repo-shortlist code-graph tool) has already been run here, unprompted, same pattern as Understand-Anything showing up elsewhere. There's also an active plan already in motion: `docs/plans/2026-07-23-graph-audit-followup.md`, described in `CLAUDE.md` as needing review before structural changes — read that first if any refactor is planned.

## Tasks
- [ ] Review `docs/plans/2026-07-23-graph-audit-followup.md` before any structural change to this repo — **Verify:** read and acknowledged
- [ ] Humanizing content pass — before building anything, check whether `Taste-Skill`/`stop-slop` actually apply to marketing copy the way they were shown to (via a separate project's own prior tooling review) — **Verify:** a scoped decision on what, if anything, gets adopted, not assumed
- [ ] No other open items surfaced — this is a stable, working static site with no stated infra pain point
